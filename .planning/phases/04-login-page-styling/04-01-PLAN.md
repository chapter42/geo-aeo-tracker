---
phase: 04-login-page-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/login/page.tsx
autonomous: false
requirements:
  - AUTH-07

must_haves:
  truths:
    - "The login page card renders on the themed card surface (not raw dark gray)"
    - "The password input uses bd-input focus ring and border, matching the rest of the app"
    - "The submit button is styled with bd-btn-primary (accent color, not hardcoded blue)"
    - "Heading and label text use th-text / th-text-secondary tokens, not text-white / text-gray-300"
    - "Error text uses text-th-danger, not text-red-500"
    - "The Suspense fallback text uses text-th-text-muted, not text-gray-400"
    - "All styled elements adapt correctly when toggling between light and dark theme"
  artifacts:
    - path: "app/login/page.tsx"
      provides: "Themed login page — all hardcoded colors replaced"
      contains: "bd-panel"
  key_links:
    - from: "app/login/page.tsx"
      to: "app/globals.css"
      via: "bd-panel, bd-input, bd-btn-primary class names"
      pattern: "bd-panel|bd-input|bd-btn-primary"
    - from: "app/login/page.tsx"
      to: "app/globals.css"
      via: "text-th-*, border-th-* Tailwind token utilities"
      pattern: "text-th-|border-th-"
---

<objective>
Replace all hardcoded Tailwind color utilities in the login page with the app's existing theme system (th-* CSS custom property tokens and bd-* component classes).

Purpose: AUTH-07 — the login page was built with static dark-mode colors (bg-gray-900, text-white, bg-blue-600, etc.) that break in light mode and don't match the app's design language. The rest of the app uses a theming system defined in globals.css; the login page should use it too.

Output: A restyled app/login/page.tsx where every color class is a theme token. No logic changes. No new files. No new dependencies.
</objective>

<execution_context>
@/Users/royhuiskes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royhuiskes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-login-page-styling/04-RESEARCH.md

<interfaces>
<!-- Key component classes defined in app/globals.css — use these, do not reconstruct them manually. -->

.bd-panel {
  background: var(--th-card);
  border: 1px solid var(--th-border);
  box-shadow: var(--th-shadow);
}

.bd-input {
  background: var(--th-card);
  border: 1px solid var(--th-border);
  color: var(--th-text);
  transition: border-color 0.15s, box-shadow 0.15s;
}
.bd-input::placeholder { color: var(--th-text-muted); }
.bd-input:focus {
  outline: none;
  border-color: var(--th-accent);
  box-shadow: 0 0 0 3px var(--th-ring);
}

.bd-btn-primary {
  background: var(--th-accent);
  color: var(--th-text-inverse);
  border: 1px solid var(--th-accent);
  font-weight: 500;
  transition: background-color 0.15s, box-shadow 0.15s;
}
.bd-btn-primary:hover { background: var(--th-accent-hover); }

<!-- Tailwind v4 token utilities available (registered via @theme inline in globals.css) -->
<!-- Surface:  bg-th-bg, bg-th-card, bg-th-card-alt, bg-th-sidebar, bg-th-inset        -->
<!-- Text:     text-th-text, text-th-text-secondary, text-th-text-muted, text-th-danger  -->
<!-- Border:   border-th-border, border-th-border-hover, border-th-accent                -->

<!-- Real usage pattern (from components/dashboard/tabs/prompt-hub-tab.tsx):
<input className="bd-input w-full rounded-lg px-3 py-2 text-sm" />
<button className="bd-btn-primary rounded-lg px-4 py-2 text-sm disabled:opacity-60">Add</button>
<div className="bd-panel rounded-lg p-6"> -->

<!-- Current login page file (app/login/page.tsx) — 100 lines, no logic changes needed:
     Line 49:  outer wrapper div — no background needed (body sets background via var(--background))
     Line 50:  card div — replace border-gray-700 bg-gray-900 with bd-panel
     Line 51:  heading — replace text-white with text-th-text
     Line 57:  label — replace text-gray-300 with text-th-text-secondary
     Line 68:  input — replace entire className string with bd-input + sizing utilities only
     Line 74:  error p — replace text-red-500 with text-th-danger
     Line 80:  button — replace entire className string with bd-btn-primary + sizing/state utilities
     Line 96:  Suspense fallback div — replace text-gray-400 with text-th-text-muted -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded colors in app/login/page.tsx with theme tokens</name>
  <files>app/login/page.tsx</files>
  <action>
    Make the following className substitutions in app/login/page.tsx. No logic, imports, state, or JSX structure changes — only className strings.

    **Card container (line 50):**
    Remove: `p-8 rounded-lg max-w-sm w-full border border-gray-700 bg-gray-900`
    Add: `bd-panel rounded-lg p-8 max-w-sm w-full`
    Note: bd-panel provides background, border, and shadow — do not add bg-th-card or border-th-border separately.

    **Heading (line 51):**
    Remove: `text-xl font-semibold mb-6 text-white`
    Add: `text-xl font-semibold mb-6 text-th-text`

    **Label (line 57):**
    Remove: `block text-sm font-medium text-gray-300 mb-1`
    Add: `block text-sm font-medium text-th-text-secondary mb-1`

    **Input (line 68):**
    Remove: `w-full px-3 py-2 border border-gray-600 rounded-md bg-gray-800 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent`
    Add: `bd-input w-full rounded-md px-3 py-2`
    Note: bd-input already handles focus ring, border, placeholder color, and focus outline. Do NOT add any focus: utilities on top of bd-input — they would conflict.

    **Error text (line 74):**
    Remove: `text-red-500 text-sm`
    Add: `text-th-danger text-sm`

    **Submit button (line 80):**
    Remove: `w-full py-2 px-4 bg-blue-600 hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed text-white font-medium rounded-md transition-colors`
    Add: `bd-btn-primary w-full rounded-md py-2 px-4 disabled:opacity-50 disabled:cursor-not-allowed`
    Note: bd-btn-primary provides background, color, border, font-weight, and hover state. Keep disabled: variants — they are state utilities, not color overrides.

    **Suspense fallback (line 96):**
    Remove: `flex items-center justify-center min-h-screen text-gray-400`
    Add: `flex items-center justify-center min-h-screen text-th-text-muted`
  </action>
  <verify>
    <automated>cd /Users/royhuiskes/geo-aeo-tracker && npx tsc --noEmit 2>&1 | grep -E "login/page" || echo "No TypeScript errors in login/page.tsx"</automated>
  </verify>
  <done>
    All 7 hardcoded color groups replaced. File contains no bg-gray-*, text-white, text-gray-*, border-gray-*, bg-blue-*, hover:bg-blue-*, text-red-500 utilities. TypeScript check passes with no errors in login/page.tsx.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — login page looks correct in both themes</name>
  <files>app/login/page.tsx</files>
  <action>
    Visually verify the restyled login page in both dark and light modes. Run `npm run dev`, visit http://localhost:3000/login, and confirm the card, input, and button all use the app's color palette (not raw grays and blue). Toggle light mode via browser console to verify theme adaptation.
  </action>
  <what-built>
    Restyled login page — all hardcoded Tailwind color classes replaced with bd-panel, bd-input, bd-btn-primary, and text-th-* / text-th-danger tokens.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open http://localhost:3000/login in a browser (or visit any page to be redirected there)
    3. **Dark mode check:** The page should show a dark navy card on a dark background. The input field should have a subtle dark border. The submit button should be the app's accent red (#FF3349), not blue.
    4. **Light mode check:** Open browser console and run `localStorage.setItem('sovereign-theme', 'light'); location.reload()`. The login page should now show a white card on a warm off-white background. Heading and label text should be dark navy (#002C48), not white.
    5. **Error state check:** Submit the form with a wrong password. The error message should appear in the danger red (theme-appropriate), not a hardcoded text-red-500.
    6. Confirm there are no visual regressions: form layout, spacing, and alignment are unchanged.
    7. Reset theme: `localStorage.setItem('sovereign-theme', 'dark'); location.reload()`
  </how-to-verify>
  <verify>Manual visual verification in browser — no automated command</verify>
  <done>User confirms login page looks correct in both dark and light themes with no regressions.</done>
  <resume-signal>Type "approved" if both themes look correct, or describe any issues for a fix</resume-signal>
</task>

</tasks>

<verification>
After Task 1 completes, confirm:
- `grep -n "bg-gray-\|text-white\|text-gray-\|border-gray-\|bg-blue-\|hover:bg-blue-\|text-red-500" app/login/page.tsx` returns no matches
- `grep -n "bd-panel\|bd-input\|bd-btn-primary\|text-th-\|text-th-danger" app/login/page.tsx` returns 6+ matches
- TypeScript compilation passes (`npx tsc --noEmit` no errors related to login/page.tsx)
</verification>

<success_criteria>
AUTH-07 is satisfied when:
1. The login page card renders using bd-panel (card surface, border, shadow from theme — not hardcoded dark gray)
2. The password input uses bd-input (focus ring, border, placeholder from theme — not hardcoded blue focus)
3. The submit button uses bd-btn-primary (accent color from theme — not hardcoded bg-blue-600)
4. Heading, labels, and error text use th-* color tokens that adapt to both light and dark themes
5. Visual verification passes in both dark mode (default) and light mode (localStorage toggle)
</success_criteria>

<output>
After completion, create `.planning/phases/04-login-page-styling/04-01-SUMMARY.md` following the summary template.
</output>
